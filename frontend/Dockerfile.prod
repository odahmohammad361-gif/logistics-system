# Production Dockerfile — builds static files served by nginx
FROM node:20-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Final stage: just the dist files
FROM alpine:3.19
COPY --from=builder /app/dist /app/dist
